{
  "name": "ai_persona_constructs",
  "build": {
    "dockerfile": "Dockerfile",
    "context": "."
  },
  "settings": {
    "terminal.integrated.defaultProfile.linux": "bash",
    "python.defaultInterpreterPath": "/workspaces/ai_persona_constructs/.venv/bin/python"
  },
  "features": {},
  "postCreateCommand": "python3 -m venv /workspaces/ai_persona_constructs/.venv && /workspaces/ai_persona_constructs/.venv/bin/python -m pip install --upgrade pip && /workspaces/ai_persona_constructs/.venv/bin/pip install -r tools/requirements.txt && sudo npm install -g @google/gemini-cli",
  "remoteUser": "vscode",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "timonwong.shellcheck"
      ]
    }
  },
  "mounts": [
    "source=${localWorkspaceFolder},target=/workspaces/ai_persona_constructs,type=bind,consistency=cached"
  ],
  "workspaceFolder": "/workspaces/ai_persona_constructs",
  "containerEnv": {
    "PYTHONUNBUFFERED": "1"
  }
}
